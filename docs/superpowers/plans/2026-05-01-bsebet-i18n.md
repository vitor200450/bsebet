# BSEBET i18n — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Full English+Portuguese internationalization via URL-path-prefix routing, react-i18next, and namespace-organized translation files across all ~43 files with user-facing strings.

**Architecture:** TanStack Router segment `$lang` wraps all routes; `$lang/__layout.tsx` syncs i18next language from URL params; components use `useTranslation(ns)` hook; server functions accept `lang` param and use a server-side `getServerT()` helper; translations live in `public/locales/{pt,en}/` as JSON namespaces by domain.

**Tech Stack:** i18next, react-i18next, TanStack Router (file-based with `$lang` segment), TanStack Start, React 19, Zod validation, Sonner toasts.

---

## Task 1: Install i18next dependencies

**Files:**
- Modify: `apps/web/package.json`

- [ ] **Step 1: Install i18next and react-i18next**

Run: `bun add i18next react-i18next --cwd apps/web`

Expected: packages added to `apps/web/package.json` and `bun.lock` updated.

- [ ] **Step 2: Commit**

```bash
git add apps/web/package.json bun.lock
git commit -m "deps: add i18next and react-i18next"
```

---

## Task 2: Create i18n infrastructure files

**Files:**
- Create: `apps/web/src/i18n/namespaces.ts`
- Create: `apps/web/src/i18n/config.ts`
- Create: `apps/web/src/i18n/index.ts`

- [ ] **Step 1: Create namespace type definitions**

Create `apps/web/src/i18n/namespaces.ts`:

```typescript
export const NAMESPACES = [
	"common",
	"betting",
	"dashboard",
	"my-bets",
	"leaderboard",
	"profile",
	"tournament",
	"team",
	"user",
	"landing",
	"admin",
	"admin-matches",
	"errors",
	"validation",
] as const;

export type AppNamespace = (typeof NAMESPACES)[number];

export const DEFAULT_NAMESPACE: AppNamespace = "common";
```

- [ ] **Step 2: Create i18next client config**

Create `apps/web/src/i18n/config.ts`:

```typescript
import { NAMESPACES } from "./namespaces";
import type { InitOptions } from "i18next";

import commonPt from "../../public/locales/pt/common.json";
import bettingPt from "../../public/locales/pt/betting.json";
import dashboardPt from "../../public/locales/pt/dashboard.json";
import myBetsPt from "../../public/locales/pt/my-bets.json";
import leaderboardPt from "../../public/locales/pt/leaderboard.json";
import profilePt from "../../public/locales/pt/profile.json";
import tournamentPt from "../../public/locales/pt/tournament.json";
import teamPt from "../../public/locales/pt/team.json";
import userPt from "../../public/locales/pt/user.json";
import landingPt from "../../public/locales/pt/landing.json";
import adminPt from "../../public/locales/pt/admin.json";
import adminMatchesPt from "../../public/locales/pt/admin-matches.json";
import errorsPt from "../../public/locales/pt/errors.json";
import validationPt from "../../public/locales/pt/validation.json";

import commonEn from "../../public/locales/en/common.json";
import bettingEn from "../../public/locales/en/betting.json";
import dashboardEn from "../../public/locales/en/dashboard.json";
import myBetsEn from "../../public/locales/en/my-bets.json";
import leaderboardEn from "../../public/locales/en/leaderboard.json";
import profileEn from "../../public/locales/en/profile.json";
import tournamentEn from "../../public/locales/en/tournament.json";
import teamEn from "../../public/locales/en/team.json";
import userEn from "../../public/locales/en/user.json";
import landingEn from "../../public/locales/en/landing.json";
import adminEn from "../../public/locales/en/admin.json";
import adminMatchesEn from "../../public/locales/en/admin-matches.json";
import errorsEn from "../../public/locales/en/errors.json";
import validationEn from "../../public/locales/en/validation.json";

export const SUPPORTED_LANGS = ["pt", "en"] as const;
export type SupportedLang = (typeof SUPPORTED_LANGS)[number];
export const DEFAULT_LANG: SupportedLang = "pt";

export const resources = {
	pt: {
		common: commonPt,
		betting: bettingPt,
		dashboard: dashboardPt,
		"my-bets": myBetsPt,
		leaderboard: leaderboardPt,
		profile: profilePt,
		tournament: tournamentPt,
		team: teamPt,
		user: userPt,
		landing: landingPt,
		admin: adminPt,
		"admin-matches": adminMatchesPt,
		errors: errorsPt,
		validation: validationPt,
	},
	en: {
		common: commonEn,
		betting: bettingEn,
		dashboard: dashboardEn,
		"my-bets": myBetsEn,
		leaderboard: leaderboardEn,
		profile: profileEn,
		tournament: tournamentEn,
		team: teamEn,
		user: userEn,
		landing: landingEn,
		admin: adminEn,
		"admin-matches": adminMatchesEn,
		errors: errorsEn,
		validation: validationEn,
	},
} as const;

export const i18nOptions: InitOptions = {
	lng: DEFAULT_LANG,
	fallbackLng: DEFAULT_LANG,
	ns: NAMESPACES as unknown as string[],
	defaultNS: "common",
	resources: resources as unknown as InitOptions["resources"],
	interpolation: { escapeValue: false },
	returnNull: false,
	returnEmptyString: false,
};

export function locale(lang: SupportedLang): string {
	return lang === "pt" ? "pt-BR" : "en-US";
}
```

- [ ] **Step 3: Create i18n init/index module**

Create `apps/web/src/i18n/index.ts`:

```typescript
import i18next from "i18next";
import { initReactI18next } from "react-i18next";
import { i18nOptions } from "./config";
import type { SupportedLang } from "./config";

let initialized = false;

export async function initI18n(lang: SupportedLang = "pt") {
	if (initialized) {
		if (i18next.language !== lang) {
			await i18next.changeLanguage(lang);
		}
		return i18next;
	}

	await i18next.use(initReactI18next).init({
		...i18nOptions,
		lng: lang,
	});

	initialized = true;
	return i18next;
}

export function changeLanguage(lang: SupportedLang) {
	return i18next.changeLanguage(lang);
}

export function getServerT(lang: SupportedLang) {
	const instance = i18next.cloneInstance();
	return instance
		.init({
			...i18nOptions,
			lng: lang,
		})
		.then(() => instance.getFixedT(lang));
}

export { i18next };
```

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/i18n/
git commit -m "feat: add i18next infrastructure (config, namespaces, init)"
```

---

## Task 3: Create pt/ translation files (common, betting, dashboard)

**Files:**
- Create: `apps/web/public/locales/pt/common.json`
- Create: `apps/web/public/locales/pt/betting.json`
- Create: `apps/web/public/locales/pt/dashboard.json`

- [ ] **Step 1: Create pt/common.json**

Create `apps/web/public/locales/pt/common.json`:

```json
{
  "appName": "BSEBET",
  "nav": {
    "home": "Inicio",
    "dashboard": "Painel",
    "myBets": "Meus Palpites",
    "leaderboard": "Ranking",
    "tournaments": "Torneios",
    "teams": "Times",
    "admin": "Admin",
    "adminUsers": "Usuarios",
    "adminTeams": "Times",
    "adminTournaments": "Torneios",
    "adminCompensations": "Compensacao",
    "adminLive": "Ao Vivo",
    "profile": "Perfil",
    "profileView": "Ver Perfil Publico"
  },
  "matchStatus": {
    "scheduled": "Agendado",
    "live": "Ao Vivo",
    "finished": "Finalizado",
    "locked": "Trancado",
    "draft": "Rascunho",
    "open": "Aberto"
  },
  "actions": {
    "save": "Salvar",
    "cancel": "Cancelar",
    "confirm": "Confirmar",
    "delete": "Excluir",
    "edit": "Editar",
    "back": "Voltar",
    "loading": "Carregando...",
    "expandAll": "Expandir todos",
    "collapseAll": "Recolher todos"
  },
  "region": "Regiao",
  "series": "Serie",
  "points": "pontos",
  "finished": "Concluido"
}
```

- [ ] **Step 2: Create pt/betting.json**

Create `apps/web/public/locales/pt/betting.json`:

```json
{
  "hero": {
    "title": "Faca seus palpites e suba no ranking",
    "subtitle": "voce entende do competitivo",
    "description": "Acumule pontos com seus palpites certeiros e escale ate o topo"
  },
  "recovery": {
    "available": "APOSTAS DE RECUPERACAO DISPONIVEIS!",
    "active": "Modo Recuperacao Ativo",
    "errorPrompt": "Voce errou algumas previsoes...",
    "qualify": "Tem direito a apostas de recuperacao!",
    "points": "recuperar pontos",
    "wrongMatchup": "Confronto diferente do palpite",
    "wrongWinner": "Seu palpite de vencedor nao bateu",
    "matchupLabel": "Voce apostou num confronto que nao ocorreu"
  },
  "bonus": {
    "underdog": "Bonus azarao (+{{percent}}%)",
    "underdogLabel": "Azaroes"
  },
  "toast": {
    "confirmed": "Suas apostas foram confirmadas! Boa sorte, campeao!",
    "updated": "Aposta atualizada com sucesso",
    "saved": "Aposta registrada com sucesso"
  },
  "empty": {
    "noTournament": "Nenhum Torneio Disponivel",
    "noMatches": "Os jogos de hoje ja comecaram ou ainda nao foram agendados."
  },
  "betLabel": "Voce apostou em:",
  "totalPoints": "Total: {{count}} pontos",
  "matchDayComplete": "Concluido!",
  "matchDayNewAvailable": "Ha um novo match day disponivel",
  "nextMatch": "Proximo Jogo",
  "betCount_one": "{{count}} palpite",
  "betCount_other": "{{count}} palpites",
  "walkoverNote": "Todos os apostadores recebem pontos de vencedor",
  "walkoverTitle": "Partida encerrada por walkover"
}
```

- [ ] **Step 3: Create pt/dashboard.json**

Create `apps/web/public/locales/pt/dashboard.json`:

```json
{
  "greeting": "Ola, {{name}}!",
  "stats": {
    "title": "Suas Estatisticas",
    "underdogsWon": "Azaroes Vencedores"
  },
  "viewHistory": "Ver historico completo",
  "empty": {
    "noBets": "Faca suas apostas e acompanhe aqui!",
    "noActive": "Nenhum palpite ativo"
  },
  "betLabel": "Seu palpite",
  "status": {
    "live": "AO VIVO",
    "scheduled": "AGENDADO"
  }
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/web/public/locales/pt/common.json apps/web/public/locales/pt/betting.json apps/web/public/locales/pt/dashboard.json
git commit -m "feat: add pt translations for common, betting, dashboard"
```

---

## Task 4: Create pt/ translation files (my-bets, leaderboard, profile, tournament)

**Files:**
- Create: `apps/web/public/locales/pt/my-bets.json`
- Create: `apps/web/public/locales/pt/leaderboard.json`
- Create: `apps/web/public/locales/pt/profile.json`
- Create: `apps/web/public/locales/pt/tournament.json`

- [ ] **Step 1: Create pt/my-bets.json**

```json
{
  "header": "Historico completo dos seus palpites",
  "tabs": {
    "all": "Todas",
    "pending": "Pendentes",
    "finished": "Finalizados",
    "recovery": "Recuperacao"
  },
  "labels": {
    "projection": "Projecao",
    "projectionTooltip": "Projecao baseada na sua performance historica",
    "futureProjection": "Projecao futura",
    "underdogLabel": "Azarao",
    "waitingScore": "Aguardando resultado",
    "selectTournament": "Selecionar torneio",
    "clickToExpand": "Clique no nome do torneio para expandir"
  },
  "empty": {
    "none": "Nenhum palpite encontrado",
    "noMatches": "Nenhuma partida encontrada",
    "cta": "Faca suas apostas nos torneios ativos!"
  },
  "actions": {
    "viewTournament": "Ver torneio"
  }
}
```

- [ ] **Step 2: Create pt/leaderboard.json**

```json
{
  "title": "Ranking",
  "rankingCriteria": "Criterios de Classificacao",
  "tabs": {
    "global": "Ranking Mundial",
    "tournament": "Camp Especifico"
  },
  "columns": {
    "position": "Posicao",
    "player": "Jogador",
    "score": "Pontuacao",
    "underdogs": "Azaroes",
    "globalPosition": "Posicao geral",
    "lastMonthResult": "Resultado mes anterior",
    "primaryCriteria": "Criterio principal"
  },
  "empty": "Nenhum dado disponivel",
  "selectTournament": "Filtrar torneio"
}
```

- [ ] **Step 3: Create pt/profile.json**

```json
{
  "title": "Perfil",
  "editInfo": "Edite suas informacoes",
  "displayNameLabel": "Nome de exibicao",
  "displayNameHint": "Seu nome de exibicao no leaderboard",
  "avatarNote": "JPG, PNG -- max. 2MB",
  "googleAccountNote": "Esses dados vem da sua conta Google e nao podem ser alterados.",
  "saveChanges": "SALVAR ALTERACOES",
  "viewPublic": "Ver Perfil Publico",
  "toast": {
    "saved": "Perfil atualizado com sucesso!",
    "error": "Erro ao salvar. Tente novamente.",
    "avatarRestoreError": "Nao foi possivel restaurar o avatar do Google. Tente sair e entrar novamente."
  },
  "userMenu": {
    "label": "Perfil do Usuario",
    "viewPublic": "Ver Perfil Publico"
  }
}
```

- [ ] **Step 4: Create pt/tournament.json**

```json
{
  "browse": {
    "description": "Competicoes de alto nivel de Brawl Stars",
    "searchPlaceholder": "Buscar torneio...",
    "empty": "Nada por aqui",
    "noResults": "Nenhum resultado",
    "noActive": "Nao ha torneios ativos no momento.",
    "noScheduled": "Nao ha torneios programados no momento.",
    "noFinished": "Nao ha torneios finalizados no momento.",
    "tabs": {
      "active": "Ativos",
      "scheduled": "Programados",
      "finished": "Finalizados"
    }
  },
  "detail": {
    "overview": "Visao Geral",
    "bracket": "Chaveamento",
    "standings": "Classificacao",
    "history": "Historico",
    "noMatches": "Nenhuma partida encontrada",
    "round": "rodada {{number}}",
    "elimination": "eliminacao"
  },
  "podium": {
    "title": "Podio Final",
    "champion": "CAMPEAO"
  },
  "selector": {
    "recovery": "RECUPERACAO",
    "label": "Selecionar torneio"
  },
  "matchDay": {
    "completed": "Concluido",
    "available": "disponivel",
    "availablePlural": "disponiveis",
    "noMatchDay": "Nao ha match days disponiveis para este torneio.",
    "selectPrompt": "Escolha qual dia de partidas voce quer visualizar",
    "noMatches": "Nenhuma partida atribuida",
    "pending": "Aguardando atualizacao"
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add apps/web/public/locales/pt/my-bets.json apps/web/public/locales/pt/leaderboard.json apps/web/public/locales/pt/profile.json apps/web/public/locales/pt/tournament.json
git commit -m "feat: add pt translations for my-bets, leaderboard, profile, tournament"
```

---

## Task 5: Create pt/ translation files (team, user, landing, admin, admin-matches, errors, validation)

**Files:**
- Create: `apps/web/public/locales/pt/team.json`
- Create: `apps/web/public/locales/pt/user.json`
- Create: `apps/web/public/locales/pt/landing.json`
- Create: `apps/web/public/locales/pt/admin.json`
- Create: `apps/web/public/locales/pt/admin-matches.json`
- Create: `apps/web/public/locales/pt/errors.json`
- Create: `apps/web/public/locales/pt/validation.json`

- [ ] **Step 1: Create pt/team.json**

```json
{
  "stats": {
    "winRate": "Taxa de Vitoria",
    "wins": "Vitorias",
    "currentStreak": "Sequencia Atual"
  },
  "sections": {
    "upcoming": "Proximas Partidas",
    "recentHistory": "Historico Recente"
  },
  "empty": {
    "noFinishedMatches": "Nenhuma partida finalizada"
  }
}
```

- [ ] **Step 2: Create pt/user.json**

```json
{
  "stats": {
    "general": "ESTATISTICAS GERAIS",
    "underdogs": "Azaroes",
    "tournamentHistory": "Historico de Torneios",
    "filterTournament": "Filtrar torneio"
  },
  "medals": {
    "latest": "Ultimas Conquistas"
  }
}
```

- [ ] **Step 3: Create pt/landing.json**

```json
{
  "hero": {
    "tagline": "FACA SEUS",
    "taglineHighlight": "PALPITES",
    "subtitle": "e suba no ranking!",
    "loginCta": "Faca login para comecar a apostar"
  },
  "features": {
    "predict": "Preveja resultados",
    "predictDesc": "voce entende do competitivo.",
    "score": "Acumule pontos",
    "scoreDesc": "com seus palpites certeiros",
    "prizes": "PREMIO"
  }
}
```

- [ ] **Step 4: Create pt/admin.json**

```json
{
  "common": {
    "title": "Administracao",
    "users": "Usuarios",
    "teams": "Times",
    "tournaments": "Torneios",
    "compensations": "Compensacao de Pontos",
    "migrateLogos": "Migracao de Logos",
    "logoMigrationPage": "Pagina de Migracao"
  },
  "users": {
    "title": "Gerenciar Usuarios"
  },
  "teams": {
    "title": "Gerenciar Times",
    "create": "Novo Time",
    "edit": "Editar Time",
    "delete": "Excluir Time",
    "deleteConfirm": "Confirmar Exclusao",
    "deleteCancel": "Nao, Cancelar",
    "region": "Regiao",
    "logoTooLarge": "O arquivo e muito grande! Maximo 500KB.",
    "base64Warning": "Esta logo esta em Base64. Salve para converter para objeto de storage.",
    "toast": {
      "deleted": "Time excluido!",
      "saveError": "Erro ao salvar time",
      "deleteError": "Erro ao excluir time"
    }
  },
  "tournaments": {
    "title": "Gerenciar Torneios",
    "create": "Novo Torneio",
    "edit": "Editar Torneio",
    "underdogLimits": "Limites de underdog invalidos: {{details}}",
    "tier1Bonus": "Bonus do tier 1 de underdog",
    "tier2Bonus": "Bonus do tier 2 de underdog",
    "tier1Threshold": "Tier 1 ate (%)",
    "tier2Threshold": "Tier 2 ate (%)",
    "maxVotes": "Limite maximo de votos",
    "autoFallback": "Fallback automatico",
    "base64Warning": "Esta logo esta em Base64. Salve para converter para objeto de storage."
  },
  "compensations": {
    "title": "COMPENSACAO DE PONTOS",
    "new": "Nova Compensacao",
    "user": "Usuario",
    "searchUser": "Buscar usuario por nome ou email...",
    "points": "Pontos",
    "reason": "Motivo",
    "reasonPlaceholder": "Descreva o motivo da compensacao...",
    "confirm": "Confirmar Compensacao",
    "auditNote": "Esta acao sera registrada e e irreversivel. Apenas administradores podem realizar compensacoes.",
    "irreversible": "Esta acao nao pode ser desfeita.",
    "history": "Historico de Ajustes",
    "description": "Ajuste manual de pontos para usuarios afetados por bugs ou erros de sistema."
  },
  "migrateLogos": {
    "description": "O que sera feito?",
    "warning": "Atencao: Este processo modifica o banco de dados e nao pode ser desfeito. Certifique-se de ter um backup recente antes de prosseguir.",
    "start": "Iniciar Migracao",
    "running": "Executando...",
    "success": "Migracao Concluida",
    "error": "Erro na Migracao",
    "completedAt": "Concluido em: {{time}}"
  }
}
```

- [ ] **Step 5: Create pt/admin-matches.json**

```json
{
  "matchBuilder": {
    "title": "Gerenciar Partidas",
    "description": "Crie e organize as partidas visualmente para ficarem identicas a disposicao do torneio oficial.",
    "groupStage": "Fase de Grupos",
    "manageGroupMatches": "Gerencie as partidas e resultados da fase de grupos",
    "existingMatchesWarning": "ATENCAO: Ja existem partidas de grupo geradas. Gerar novamente REMOVERA todas as partidas existentes e as apostas associadas.",
    "continueQuestion": "Deseja continuar?"
  },
  "reset": {
    "confirm": "Tem certeza que deseja resetar esta partida? Todas as apostas serao removidas.",
    "resetAllConfirm": "Tem certeza que deseja resetar TODAS as partidas deste torneio? Todas as apostas serao removidas."
  },
  "recalc": {
    "button": "Recalcular Pontuacao",
    "success": "Recalculo concluido: {{matchCount}} partidas, {{betCount}} apostas",
    "error": "Erro ao recalcular pontuacao do torneio"
  },
  "modal": {
    "saveError": "Falha ao salvar atualizacao da partida",
    "woDescription": "W.O. com um unico time definido sera resolvido como vitoria automatica do time presente.",
    "woUpdated": "Vencedor do W.O. atualizado com sucesso",
    "clearWinner": "Limpar vencedor"
  },
  "live": {
    "tieWarning": "O placar esta empatado. Defina um vencedor.",
    "teamsError": "Erro: Times nao definidos",
    "scoringStarted": "Voce comecou a pontuar esta partida.",
    "irreversibleWarning": "Esta acao nao pode ser desfeita automaticamente.",
    "whatToDo": "O que deseja fazer com as alteracoes?"
  },
  "matchDays": {
    "title": "Match Days",
    "created": "Dia criado com sucesso!",
    "deleted": "Dia excluido.",
    "createError": "Erro ao criar dia.",
    "deleteError": "Erro ao excluir dia.",
    "updateError": "Erro ao atualizar match day.",
    "draft": "Draft = Ainda nao disponivel"
  },
  "ordering": {
    "saved": "Ordem salva com sucesso!",
    "saveError": "Erro ao salvar ordem"
  }
}
```

- [ ] **Step 6: Create pt/errors.json**

```json
{
  "unauthenticated": "Usuario nao autenticado",
  "matchNotFound": "Partida nao encontrada",
  "tournamentNotFound": "Torneio nao encontrado",
  "teamNotFound": "Time nao encontrado",
  "userNotFound": "Usuario nao encontrado",
  "bettingClosed": "Apostas encerradas - a partida ja comecou",
  "bettingDisabled": "Apostas desabilitadas para esta partida",
  "invalidWinner": "Vencedor previsto deve ser um dos times da partida",
  "invalidWinnerAlt": "Vencedor invalido para partida",
  "winnerMustHaveMorePoints": "O vencedor deve ter mais pontos que o perdedor",
  "validationErrors": "Erros de validacao",
  "betsSaved": "aposta(s) salva(s) com sucesso",
  "recoveryUnavailable": "Partida indisponivel para recovery no estado atual",
  "recoveryAlreadyPlaced": "Aposta de recuperacao ja foi realizada e nao pode ser alterada.",
  "cannotDeleteTeam": "Nao e possivel excluir este time porque ele esta vinculado aos torneios: {{tournaments}}",
  "googleAccountNotFound": "Conta Google nao encontrada",
  "googleAvatarError": "Nao foi possivel obter o avatar do Google. Tente sair e entrar novamente.",
  "adminOnly": "Apenas administradores podem ajustar pontos",
  "adminOnlySearch": "Apenas administradores podem buscar usuarios",
  "pointAdjustmentError": "Erro ao buscar ajustes de pontos",
  "pointAdjustmentFallbackUser": "Usuario"
}
```

- [ ] **Step 7: Create pt/validation.json**

```json
{
  "nameRequired": "Nome e obrigatorio",
  "slugRequired": "Slug e obrigatorio",
  "imageUrlRequired": "Deve ser uma URL valida ou imagem Base64",
  "matchIdPositive": "Match ID deve ser um numero positivo",
  "winnerIdPositive": "Winner ID deve ser um numero positivo",
  "negativeScore": "O placar nao pode ser negativo",
  "maxSets": "Maximo de {{n}} sets neste formato",
  "someoneMustWin": "Alguem precisa vencer {{n}} sets",
  "scoreLogic": "Logica inconsistente: o vencedor deve ter exatamente {{n}} sets ganhos"
}
```

- [ ] **Step 8: Commit**

```bash
git add apps/web/public/locales/pt/
git commit -m "feat: add pt translations for team, user, landing, admin, admin-matches, errors, validation"
```

---

## Task 6: Create en/ translation files (all namespaces)

**Files:**
- Create: `apps/web/public/locales/en/common.json`
- Create: `apps/web/public/locales/en/betting.json`
- Create: `apps/web/public/locales/en/dashboard.json`
- Create: `apps/web/public/locales/en/my-bets.json`
- Create: `apps/web/public/locales/en/leaderboard.json`
- Create: `apps/web/public/locales/en/profile.json`
- Create: `apps/web/public/locales/en/tournament.json`
- Create: `apps/web/public/locales/en/team.json`
- Create: `apps/web/public/locales/en/user.json`
- Create: `apps/web/public/locales/en/landing.json`
- Create: `apps/web/public/locales/en/admin.json`
- Create: `apps/web/public/locales/en/admin-matches.json`
- Create: `apps/web/public/locales/en/errors.json`
- Create: `apps/web/public/locales/en/validation.json`

- [ ] **Step 1: Create en/common.json**

Create `apps/web/public/locales/en/common.json`:

```json
{
  "appName": "BSEBET",
  "nav": {
    "home": "Home",
    "dashboard": "Dashboard",
    "myBets": "My Bets",
    "leaderboard": "Leaderboard",
    "tournaments": "Tournaments",
    "teams": "Teams",
    "admin": "Admin",
    "adminUsers": "Users",
    "adminTeams": "Teams",
    "adminTournaments": "Tournaments",
    "adminCompensations": "Compensations",
    "adminLive": "Live",
    "profile": "Profile",
    "profileView": "View Public Profile"
  },
  "matchStatus": {
    "scheduled": "Scheduled",
    "live": "Live",
    "finished": "Finished",
    "locked": "Locked",
    "draft": "Draft",
    "open": "Open"
  },
  "actions": {
    "save": "Save",
    "cancel": "Cancel",
    "confirm": "Confirm",
    "delete": "Delete",
    "edit": "Edit",
    "back": "Back",
    "loading": "Loading...",
    "expandAll": "Expand all",
    "collapseAll": "Collapse all"
  },
  "region": "Region",
  "series": "Series",
  "points": "points",
  "finished": "Finished"
}
```

- [ ] **Step 2: Create en/betting.json**

```json
{
  "hero": {
    "title": "Make your picks and climb the rankings",
    "subtitle": "you know the competitive scene",
    "description": "Earn points with your winning bets and climb to the top"
  },
  "recovery": {
    "available": "RECOVERY BETS AVAILABLE!",
    "active": "Recovery Mode Active",
    "errorPrompt": "You missed some predictions...",
    "qualify": "You qualify for recovery bets!",
    "points": "recover points",
    "wrongMatchup": "Different matchup than predicted",
    "wrongWinner": "Your winner pick didn't match",
    "matchupLabel": "You bet on a matchup that didn't happen"
  },
  "bonus": {
    "underdog": "Underdog bonus (+{{percent}}%)",
    "underdogLabel": "Underdogs"
  },
  "toast": {
    "confirmed": "Your bets are confirmed! Good luck, champion!",
    "updated": "Bet updated successfully",
    "saved": "Bet placed successfully"
  },
  "empty": {
    "noTournament": "No Tournament Available",
    "noMatches": "Today's games have already started or haven't been scheduled yet."
  },
  "betLabel": "You bet on:",
  "totalPoints": "Total: {{count}} points",
  "matchDayComplete": "Complete!",
  "matchDayNewAvailable": "A new match day is available",
  "nextMatch": "Next Match",
  "betCount_one": "{{count}} bet",
  "betCount_other": "{{count}} bets",
  "walkoverNote": "All bettors receive winner points",
  "walkoverTitle": "Match ended by walkover"
}
```

- [ ] **Step 3: Create en/dashboard.json**

```json
{
  "greeting": "Hello, {{name}}!",
  "stats": {
    "title": "Your Stats",
    "underdogsWon": "Winning Underdogs"
  },
  "viewHistory": "View full history",
  "empty": {
    "noBets": "Place your bets and track them here!",
    "noActive": "No active bets"
  },
  "betLabel": "Your bet",
  "status": {
    "live": "LIVE",
    "scheduled": "SCHEDULED"
  }
}
```

- [ ] **Step 4: Create en/my-bets.json**

```json
{
  "header": "Your complete bet history",
  "tabs": {
    "all": "All",
    "pending": "Pending",
    "finished": "Finished",
    "recovery": "Recovery"
  },
  "labels": {
    "projection": "Projection",
    "projectionTooltip": "Projection based on your historical performance",
    "futureProjection": "Future projection",
    "underdogLabel": "Underdog",
    "waitingScore": "Awaiting result",
    "selectTournament": "Select tournament",
    "clickToExpand": "Click tournament name to expand"
  },
  "empty": {
    "none": "No bets found",
    "noMatches": "No matches found",
    "cta": "Place your bets on active tournaments!"
  },
  "actions": {
    "viewTournament": "View tournament"
  }
}
```

- [ ] **Step 5: Create en/leaderboard.json**

```json
{
  "title": "Leaderboard",
  "rankingCriteria": "Ranking Criteria",
  "tabs": {
    "global": "Global Ranking",
    "tournament": "By Tournament"
  },
  "columns": {
    "position": "Position",
    "player": "Player",
    "score": "Score",
    "underdogs": "Underdogs",
    "globalPosition": "Overall position",
    "lastMonthResult": "Previous month result",
    "primaryCriteria": "Primary criteria"
  },
  "empty": "No data available",
  "selectTournament": "Filter tournament"
}
```

- [ ] **Step 6: Create en/profile.json**

```json
{
  "title": "Profile",
  "editInfo": "Edit your information",
  "displayNameLabel": "Display name",
  "displayNameHint": "Your display name on the leaderboard",
  "avatarNote": "JPG, PNG -- max 2MB",
  "googleAccountNote": "This data comes from your Google account and cannot be changed.",
  "saveChanges": "SAVE CHANGES",
  "viewPublic": "View Public Profile",
  "toast": {
    "saved": "Profile updated successfully!",
    "error": "Error saving. Please try again.",
    "avatarRestoreError": "Could not restore Google avatar. Try signing out and back in."
  },
  "userMenu": {
    "label": "User Profile",
    "viewPublic": "View Public Profile"
  }
}
```

- [ ] **Step 7: Create en/tournament.json**

```json
{
  "browse": {
    "description": "High-level Brawl Stars competitions",
    "searchPlaceholder": "Search tournaments...",
    "empty": "Nothing here",
    "noResults": "No results",
    "noActive": "No active tournaments at the moment.",
    "noScheduled": "No scheduled tournaments at the moment.",
    "noFinished": "No finished tournaments at the moment.",
    "tabs": {
      "active": "Active",
      "scheduled": "Scheduled",
      "finished": "Finished"
    }
  },
  "detail": {
    "overview": "Overview",
    "bracket": "Bracket",
    "standings": "Standings",
    "history": "History",
    "noMatches": "No matches found",
    "round": "round {{number}}",
    "elimination": "elimination"
  },
  "podium": {
    "title": "Final Podium",
    "champion": "CHAMPION"
  },
  "selector": {
    "recovery": "RECOVERY",
    "label": "Select tournament"
  },
  "matchDay": {
    "completed": "Completed",
    "available": "available",
    "availablePlural": "available",
    "noMatchDay": "No match days available for this tournament.",
    "selectPrompt": "Choose which match day you want to view",
    "noMatches": "No matches assigned",
    "pending": "Awaiting update"
  }
}
```

- [ ] **Step 8: Create en/team.json**

```json
{
  "stats": {
    "winRate": "Win Rate",
    "wins": "Wins",
    "currentStreak": "Current Streak"
  },
  "sections": {
    "upcoming": "Upcoming Matches",
    "recentHistory": "Recent History"
  },
  "empty": {
    "noFinishedMatches": "No finished matches"
  }
}
```

- [ ] **Step 9: Create en/user.json**

```json
{
  "stats": {
    "general": "GENERAL STATS",
    "underdogs": "Underdogs",
    "tournamentHistory": "Tournament History",
    "filterTournament": "Filter tournament"
  },
  "medals": {
    "latest": "Latest Achievements"
  }
}
```

- [ ] **Step 10: Create en/landing.json**

```json
{
  "hero": {
    "tagline": "MAKE YOUR",
    "taglineHighlight": "PICKS",
    "subtitle": "and climb the leaderboard!",
    "loginCta": "Sign in to start betting"
  },
  "features": {
    "predict": "Predict results",
    "predictDesc": "you know the competitive scene.",
    "score": "Earn points",
    "scoreDesc": "with your winning bets",
    "prizes": "PRIZES"
  }
}
```

- [ ] **Step 11: Create en/admin.json**

```json
{
  "common": {
    "title": "Administration",
    "users": "Users",
    "teams": "Teams",
    "tournaments": "Tournaments",
    "compensations": "Point Compensation",
    "migrateLogos": "Logo Migration",
    "logoMigrationPage": "Migration Page"
  },
  "users": {
    "title": "Manage Users"
  },
  "teams": {
    "title": "Manage Teams",
    "create": "New Team",
    "edit": "Edit Team",
    "delete": "Delete Team",
    "deleteConfirm": "Confirm Deletion",
    "deleteCancel": "No, Cancel",
    "region": "Region",
    "logoTooLarge": "File is too large! Maximum 500KB.",
    "base64Warning": "This logo is in Base64. Save to convert to storage object.",
    "toast": {
      "deleted": "Team deleted!",
      "saveError": "Error saving team",
      "deleteError": "Error deleting team"
    }
  },
  "tournaments": {
    "title": "Manage Tournaments",
    "create": "New Tournament",
    "edit": "Edit Tournament",
    "underdogLimits": "Invalid underdog limits: {{details}}",
    "tier1Bonus": "Underdog tier 1 bonus",
    "tier2Bonus": "Underdog tier 2 bonus",
    "tier1Threshold": "Tier 1 threshold (%)",
    "tier2Threshold": "Tier 2 threshold (%)",
    "maxVotes": "Maximum vote limit",
    "autoFallback": "Auto fallback",
    "base64Warning": "This logo is in Base64. Save to convert to storage object."
  },
  "compensations": {
    "title": "POINT COMPENSATION",
    "new": "New Compensation",
    "user": "User",
    "searchUser": "Search user by name or email...",
    "points": "Points",
    "reason": "Reason",
    "reasonPlaceholder": "Describe the reason for the compensation...",
    "confirm": "Confirm Compensation",
    "auditNote": "This action will be logged and is irreversible. Only administrators can perform compensations.",
    "irreversible": "This action cannot be undone.",
    "history": "Adjustment History",
    "description": "Manual point adjustment for users affected by bugs or system errors."
  },
  "migrateLogos": {
    "description": "What will be done?",
    "warning": "Warning: This process modifies the database and cannot be undone. Make sure you have a recent backup before proceeding.",
    "start": "Start Migration",
    "running": "Running...",
    "success": "Migration Complete",
    "error": "Migration Error",
    "completedAt": "Completed at: {{time}}"
  }
}
```

- [ ] **Step 12: Create en/admin-matches.json**

```json
{
  "matchBuilder": {
    "title": "Manage Matches",
    "description": "Create and organize matches visually to match the official tournament layout.",
    "groupStage": "Group Stage",
    "manageGroupMatches": "Manage group stage matches and results",
    "existingMatchesWarning": "WARNING: Group matches already exist. Regenerating will REMOVE all existing matches and associated bets.",
    "continueQuestion": "Do you want to continue?"
  },
  "reset": {
    "confirm": "Are you sure you want to reset this match? All bets will be removed.",
    "resetAllConfirm": "Are you sure you want to reset ALL matches in this tournament? All bets will be removed."
  },
  "recalc": {
    "button": "Recalculate Scores",
    "success": "Recalculation complete: {{matchCount}} matches, {{betCount}} bets",
    "error": "Error recalculating tournament scores"
  },
  "modal": {
    "saveError": "Failed to save match update",
    "woDescription": "Walkover with a single defined team will be resolved as an automatic win for the present team.",
    "woUpdated": "Walkover winner updated successfully",
    "clearWinner": "Clear winner"
  },
  "live": {
    "tieWarning": "The score is tied. Set a winner.",
    "teamsError": "Error: Teams not defined",
    "scoringStarted": "You've started scoring this match.",
    "irreversibleWarning": "This action cannot be undone automatically.",
    "whatToDo": "What would you like to do with the changes?"
  },
  "matchDays": {
    "title": "Match Days",
    "created": "Day created successfully!",
    "deleted": "Day deleted.",
    "createError": "Error creating day.",
    "deleteError": "Error deleting day.",
    "updateError": "Error updating match day.",
    "draft": "Draft = Not yet available"
  },
  "ordering": {
    "saved": "Order saved successfully!",
    "saveError": "Error saving order"
  }
}
```

- [ ] **Step 13: Create en/errors.json**

```json
{
  "unauthenticated": "Not authenticated",
  "matchNotFound": "Match not found",
  "tournamentNotFound": "Tournament not found",
  "teamNotFound": "Team not found",
  "userNotFound": "User not found",
  "bettingClosed": "Betting closed - the match has already started",
  "bettingDisabled": "Betting is disabled for this match",
  "invalidWinner": "Predicted winner must be one of the teams in the match",
  "invalidWinnerAlt": "Invalid winner for the match",
  "winnerMustHaveMorePoints": "The winner must have more points than the loser",
  "validationErrors": "Validation errors",
  "betsSaved": "bet(s) saved successfully",
  "recoveryUnavailable": "Match unavailable for recovery in current state",
  "recoveryAlreadyPlaced": "Recovery bet has already been placed and cannot be changed.",
  "cannotDeleteTeam": "Cannot delete this team because it is linked to tournaments: {{tournaments}}",
  "googleAccountNotFound": "Google account not found",
  "googleAvatarError": "Could not get Google avatar. Try signing out and back in.",
  "adminOnly": "Only administrators can adjust points",
  "adminOnlySearch": "Only administrators can search users",
  "pointAdjustmentError": "Error fetching point adjustments",
  "pointAdjustmentFallbackUser": "User"
}
```

- [ ] **Step 14: Create en/validation.json**

```json
{
  "nameRequired": "Name is required",
  "slugRequired": "Slug is required",
  "imageUrlRequired": "Must be a valid URL or Base64 image",
  "matchIdPositive": "Match ID must be a positive number",
  "winnerIdPositive": "Winner ID must be a positive number",
  "negativeScore": "Score cannot be negative",
  "maxSets": "Maximum of {{n}} sets in this format",
  "someoneMustWin": "Someone must win {{n}} sets",
  "scoreLogic": "Inconsistent logic: winner must have exactly {{n}} sets won"
}
```

- [ ] **Step 15: Commit**

```bash
git add apps/web/public/locales/en/
git commit -m "feat: add en translations for all namespaces"
```

---

## Task 7: Restructure route tree — create $lang layout and catch-all

**Files:**
- Create: `apps/web/src/routes/$.tsx`
- Create: `apps/web/src/routes/$lang/__layout.tsx`
- Modify: `apps/web/src/routes/__root.tsx`

Before moving route files, we need the `$lang` layout and root redirect ready.

- [ ] **Step 1: Create catch-all redirect for root `/`**

Create `apps/web/src/routes/$.tsx`:

```typescript
import { createFileRoute, redirect } from "@tanstack/react-router";

export const Route = createFileRoute("/$")({
	loader: () => {
		throw redirect({ to: "/pt" });
	},
});
```

- [ ] **Step 2: Create $lang/__layout.tsx**

Create `apps/web/src/routes/$lang/__layout.tsx`:

```typescript
import { createFileRoute, Outlet } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { I18nextProvider } from "react-i18next";
import { initI18n, i18next } from "@/i18n";
import { SUPPORTED_LANGS } from "@/i18n/config";
import type { SupportedLang } from "@/i18n/config";

export const Route = createFileRoute("/$lang")({
	loader: ({ params }) => {
		const lang = params.lang;
		if (!SUPPORTED_LANGS.includes(lang as SupportedLang)) {
			throw redirect({ to: "/pt" });
		}
		return { lang: lang as SupportedLang };
	},
	component: LangLayout,
});

function LangLayout() {
	const { lang } = Route.useLoaderData();
	const [ready, setReady] = useState(false);

	useEffect(() => {
		initI18n(lang).then(() => setReady(true));
	}, [lang]);

	if (!ready) return null;

	return (
		<I18nextProvider i18n={i18next}>
			<Outlet />
		</I18nextProvider>
	);
}
```

- [ ] **Step 3: Update __root.tsx to use dynamic lang**

`apps/web/src/routes/__root.tsx` — replace the `RootDocument` function:

```typescript
// Add imports at top:
import { I18nextProvider } from "react-i18next";
import { i18next } from "@/i18n";

// Replace the component function:
function RootDocument() {
	const location = useLocation();
	const lang = location.pathname.split("/")[1] === "en" ? "en" : "pt";
	return (
		<html lang={lang} className="dark" suppressHydrationWarning>
			<head suppressHydrationWarning>
				<HeadContent />
			</head>
			<body suppressHydrationWarning>
				<HeaderProvider>
					<div className="min-h-screen w-full overflow-x-hidden font-sans">
						<GlobalHeader />
						<Outlet />
					</div>
					<Toaster richColors />
					<Scripts />
					<SpeedInsights route={location.pathname} />
				</HeaderProvider>
			</body>
		</html>
	);
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/routes/$.tsx apps/web/src/routes/$lang/__layout.tsx apps/web/src/routes/__root.tsx
git commit -m "feat: add $lang route layout, root redirect, dynamic html lang"
```

---

## Task 8: Move route files into $lang/ directory

**Files:**
- Move: `apps/web/src/routes/index.tsx` → `apps/web/src/routes/$lang/index.tsx`
- Move: `apps/web/src/routes/landing.tsx` → `apps/web/src/routes/$lang/landing.tsx`
- Move: `apps/web/src/routes/dashboard.tsx` → `apps/web/src/routes/$lang/dashboard.tsx`
- Move: `apps/web/src/routes/my-bets.tsx` → `apps/web/src/routes/$lang/my-bets.tsx`
- Move: `apps/web/src/routes/leaderboard.tsx` → `apps/web/src/routes/$lang/leaderboard.tsx`
- Move: `apps/web/src/routes/login.tsx` → `apps/web/src/routes/$lang/login.tsx`
- Move: `apps/web/src/routes/profile.tsx` → `apps/web/src/routes/$lang/profile.tsx`
- Move: `apps/web/src/routes/tournaments/index.tsx` → `apps/web/src/routes/$lang/tournaments/index.tsx`
- Move: `apps/web/src/routes/tournaments/$slug.tsx` → `apps/web/src/routes/$lang/tournaments/$slug.tsx`
- Move: `apps/web/src/routes/teams/$teamId.tsx` → `apps/web/src/routes/$lang/teams/$teamId.tsx`
- Move: `apps/web/src/routes/users/$userId.tsx` → `apps/web/src/routes/$lang/users/$userId.tsx`
- Move: `apps/web/src/routes/admin/tournaments/index.tsx` → `apps/web/src/routes/$lang/admin/tournaments/index.tsx`
- Move: `apps/web/src/routes/admin/tournaments/$tournamentId/matches.tsx` → `apps/web/src/routes/$lang/admin/tournaments/$tournamentId/matches.tsx`
- Move: `apps/web/src/routes/admin/teams.tsx` → `apps/web/src/routes/$lang/admin/teams.tsx`
- Move: `apps/web/src/routes/admin/users.tsx` → `apps/web/src/routes/$lang/admin/users.tsx`
- Move: `apps/web/src/routes/admin/compensations.tsx` → `apps/web/src/routes/$lang/admin/compensations.tsx`
- Move: `apps/web/src/routes/admin/live/$matchId.tsx` → `apps/web/src/routes/$lang/admin/live/$matchId.tsx`
- Move: `apps/web/src/routes/admin/migrate-logos.tsx` → `apps/web/src/routes/$lang/admin/migrate-logos.tsx`

- [ ] **Step 1: Move all route files into $lang/ directory**

Run:

```powershell
# Create target directories
New-Item -ItemType Directory -Force -Path "apps/web/src/routes/$lang/tournaments"
New-Item -ItemType Directory -Force -Path "apps/web/src/routes/$lang/teams"
New-Item -ItemType Directory -Force -Path "apps/web/src/routes/$lang/users"
New-Item -ItemType Directory -Force -Path "apps/web/src/routes/$lang/admin/tournaments"
New-Item -ItemType Directory -Force -Path "apps/web/src/routes/$lang/admin/live"

# Move public route files
Move-Item "apps/web/src/routes/index.tsx" "apps/web/src/routes/$lang/index.tsx"
Move-Item "apps/web/src/routes/landing.tsx" "apps/web/src/routes/$lang/landing.tsx"
Move-Item "apps/web/src/routes/dashboard.tsx" "apps/web/src/routes/$lang/dashboard.tsx"
Move-Item "apps/web/src/routes/my-bets.tsx" "apps/web/src/routes/$lang/my-bets.tsx"
Move-Item "apps/web/src/routes/leaderboard.tsx" "apps/web/src/routes/$lang/leaderboard.tsx"
Move-Item "apps/web/src/routes/login.tsx" "apps/web/src/routes/$lang/login.tsx"
Move-Item "apps/web/src/routes/profile.tsx" "apps/web/src/routes/$lang/profile.tsx"

# Move tournament routes
Move-Item "apps/web/src/routes/tournaments/index.tsx" "apps/web/src/routes/$lang/tournaments/index.tsx"
Move-Item "apps/web/src/routes/tournaments/$slug.tsx" "apps/web/src/routes/$lang/tournaments/$slug.tsx"

# Move team and user routes
Move-Item "apps/web/src/routes/teams/$teamId.tsx" "apps/web/src/routes/$lang/teams/$teamId.tsx"
Move-Item "apps/web/src/routes/users/$userId.tsx" "apps/web/src/routes/$lang/users/$userId.tsx"

# Move admin routes
Move-Item "apps/web/src/routes/admin/tournaments/index.tsx" "apps/web/src/routes/$lang/admin/tournaments/index.tsx"
Move-Item "apps/web/src/routes/admin/tournaments/$tournamentId/matches.tsx" "apps/web/src/routes/$lang/admin/tournaments/$tournamentId/matches.tsx"
Move-Item "apps/web/src/routes/admin/teams.tsx" "apps/web/src/routes/$lang/admin/teams.tsx"
Move-Item "apps/web/src/routes/admin/users.tsx" "apps/web/src/routes/$lang/admin/users.tsx"
Move-Item "apps/web/src/routes/admin/compensations.tsx" "apps/web/src/routes/$lang/admin/compensations.tsx"
Move-Item "apps/web/src/routes/admin/live/$matchId.tsx" "apps/web/src/routes/$lang/admin/live/$matchId.tsx"
Move-Item "apps/web/src/routes/admin/migrate-logos.tsx" "apps/web/src/routes/$lang/admin/migrate-logos.tsx"
```

- [ ] **Step 2: Clean up empty directories**

Run:

```powershell
Remove-Item "apps/web/src/routes/tournaments" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item "apps/web/src/routes/teams" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item "apps/web/src/routes/users" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item "apps/web/src/routes/admin" -Recurse -Force -ErrorAction SilentlyContinue
```

- [ ] **Step 3: Keep untouched files in place**

These files remain at their original locations (no Portuguese strings, no translation needed):
- `apps/web/src/routes/api/trpc/$.ts`
- `apps/web/src/routes/api/auth/$.ts`

Move the `tournaments.tsx` shim:
```powershell
Move-Item "apps/web/src/routes/tournaments.tsx" "apps/web/src/routes/$lang/tournaments.tsx"
```

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/routes/
git commit -m "refactor: move all route files under $lang/ segment"
```

---

## Task 9: Refactor public route files to use i18n (part 1 — index, dashboard, my-bets, leaderboard)

**Files:**
- Modify: `apps/web/src/routes/$lang/index.tsx`
- Modify: `apps/web/src/routes/$lang/dashboard.tsx`
- Modify: `apps/web/src/routes/$lang/my-bets.tsx`
- Modify: `apps/web/src/routes/$lang/leaderboard.tsx`

- [ ] **Step 1: Read current files to identify all hardcoded strings**

Read each file and identify every Portuguese string literal.

- [ ] **Step 2: Refactor $lang/index.tsx**

Replace all Portuguese strings with `t()` calls using `useTranslation("betting")`. Key replacements:

```
"APOSTAS DE RECUPERAÇÃO DISPONÍVEIS!" → t("recovery.available")
"Voce errou algumas previsoes..." → t("recovery.errorPrompt")
"Modo Recuperacao Ativo" → t("recovery.active")
"recuperar pontos" → t("recovery.points")
"Confronto diferente do palpite" → t("recovery.wrongMatchup")
"Seu palpite de vencedor nao bateu" → t("recovery.wrongWinner")
"Voce apostou num confronto que nao ocorreu" → t("recovery.matchupLabel")
"Bonus azarao (+25%)" → t("bonus.underdog", { percent: 25 })
"Azaroes" → t("bonus.underdogLabel")
"Suas apostas foram confirmadas! Boa sorte, campeao!" → t("toast.confirmed")
"Nenhum Torneio Disponivel" → t("empty.noTournament")
"Concluido!" → t("matchDayComplete")
"Ha um novo match day disponivel" → t("matchDayNewAvailable")
"Total: 0 pontos" → t("totalPoints", { count: 0 })
"1 palpite" / "N palpites" → t("betCount", { count: N })
"Voce apostou em:" → t("betLabel")
```

Add at top:
```typescript
import { useTranslation } from "react-i18next";
```

In component:
```typescript
const { t } = useTranslation("betting");
```

- [ ] **Step 3: Refactor $lang/dashboard.tsx**

Key replacements using `useTranslation("dashboard")`:

```
"Ola, {name}!" → t("greeting", { name })
"Suas Estatisticas" → t("stats.title")
"Azaroes Vencedores" → t("stats.underdogsWon")
"Ver historico completo" → t("viewHistory")
"Faca suas apostas e acompanhe aqui!" → t("empty.noBets")
"Nenhum palpite ativo" → t("empty.noActive")
"Seu palpite" → t("betLabel")
"AO VIVO" → t("status.live")
"AGENDADO" → t("status.scheduled")
```

- [ ] **Step 4: Refactor $lang/my-bets.tsx**

Key replacements using `useTranslation("my-bets")`:

```
"Historico completo dos seus palpites" → t("header")
"Projecao" → t("labels.projection")
"Projecao futura" → t("labels.futureProjection")
"Azarao" → t("labels.underdogLabel")
"Nenhum palpite encontrado" → t("empty.none")
"Nenhum palpite pendente" → t("empty.none") // reuse
"Nenhum palpite finalizado" → t("empty.none") // reuse
"Ver torneio" → t("actions.viewTournament")
"Selecionar torneio" → t("labels.selectTournament")
"Clique no nome do torneio para expandir" → t("labels.clickToExpand")
"Faca suas apostas nos torneios ativos!" → t("empty.cta")
"N palpite(s)" → use common betCount or inline t("common:betCount", { count })
```

- [ ] **Step 5: Refactor $lang/leaderboard.tsx**

Key replacements using `useTranslation("leaderboard")`:

```
"Criterios de Classificacao" → t("rankingCriteria")
"Ranking Mundial" → t("tabs.global")
"Azaroes" → t("columns.underdogs")
"Resultado mes anterior" → t("columns.lastMonthResult")
"Posicao geral" → t("columns.globalPosition")
"Nenhum dado disponivel" → t("empty")
"Criterio principal" → t("columns.primaryCriteria")
```

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/routes/$lang/index.tsx apps/web/src/routes/$lang/dashboard.tsx apps/web/src/routes/$lang/my-bets.tsx apps/web/src/routes/$lang/leaderboard.tsx
git commit -m "feat: i18n refactor for index, dashboard, my-bets, leaderboard routes"
```

---

## Task 10: Refactor public route files to use i18n (part 2 — profile, login, landing, tournaments, teams, users)

**Files:**
- Modify: `apps/web/src/routes/$lang/profile.tsx`
- Modify: `apps/web/src/routes/$lang/login.tsx`
- Modify: `apps/web/src/routes/$lang/landing.tsx`
- Modify: `apps/web/src/routes/$lang/tournaments/index.tsx`
- Modify: `apps/web/src/routes/$lang/tournaments/$slug.tsx`
- Modify: `apps/web/src/routes/$lang/teams/$teamId.tsx`
- Modify: `apps/web/src/routes/$lang/users/$userId.tsx`

- [ ] **Step 1: Refactor $lang/profile.tsx**

Use `useTranslation("profile")`:

```
"Edite suas informacoes" → t("editInfo")
"JPG, PNG -- max. 2MB" → t("avatarNote")
"Esses dados vem da sua conta Google..." → t("googleAccountNote")
"Seu nome de exibicao no leaderboard" → t("displayNameHint")
"SALVAR ALTERACOES" → t("saveChanges")
"Ver Perfil Publico" → t("viewPublic")
"Perfil atualizado com sucesso!" → t("toast.saved")
"Erro ao salvar. Tente novamente." → t("toast.error")
"Nao foi possivel restaurar o avatar..." → t("toast.avatarRestoreError")
```

- [ ] **Step 2: Refactor $lang/login.tsx**

Use `useTranslation("landing")`:

```
"Faca login para comecar a apostar" → t("hero.loginCta")
```

- [ ] **Step 3: Refactor $lang/landing.tsx**

Use `useTranslation("landing")`:

```
"FACA SEUS" → t("hero.tagline")
"PALPITES" → t("hero.taglineHighlight")
"e suba no ranking!" → t("hero.subtitle")
"voce entende do competitivo." → t("features.predictDesc")
"Acumule pontos" → t("features.score")
"com seus palpites certeiros" → t("features.scoreDesc")
"PREMIO" → t("features.prizes")
```

- [ ] **Step 4: Refactor $lang/tournaments/index.tsx**

Use `useTranslation("tournament")`:

```
"Competicoes de alto nivel de Brawl Stars" → t("browse.description")
"Buscar torneio..." → t("browse.searchPlaceholder")
"Nada por aqui" → t("browse.empty")
"Nenhum resultado" → t("browse.noResults")
"Nao ha torneios ativos..." → t("browse.noActive")
"Nao ha torneios programados..." → t("browse.noScheduled")
"Nao ha torneios finalizados..." → t("browse.noFinished")
```

- [ ] **Step 5: Refactor $lang/tournaments/$slug.tsx**

Use `useTranslation("tournament")`:

```
"Nenhuma partida encontrada" → t("detail.noMatches")
"rodada 1" → t("detail.round", { number: 1 })
"eliminacao" → t("detail.elimination")
```

- [ ] **Step 6: Refactor $lang/teams/$teamId.tsx**

Use `useTranslation("team")`:

```
"Taxa de Vitoria" → t("stats.winRate")
"Vitorias" → t("stats.wins")
"Sequencia Atual" → t("stats.currentStreak")
"Proximas Partidas" → t("sections.upcoming")
"Historico Recente" → t("sections.recentHistory")
"Nenhuma partida finalizada" → t("empty.noFinishedMatches")
```

- [ ] **Step 7: Refactor $lang/users/$userId.tsx**

Use `useTranslation("user")`:

```
"ESTATISTICAS GERAIS" → t("stats.general")
"Azaroes" → t("stats.underdogs")
"Historico de Torneios" → t("stats.tournamentHistory")
"Filtrar torneio" → t("stats.filterTournament")
```

- [ ] **Step 8: Commit**

```bash
git add apps/web/src/routes/$lang/profile.tsx apps/web/src/routes/$lang/login.tsx apps/web/src/routes/$lang/landing.tsx apps/web/src/routes/$lang/tournaments/ apps/web/src/routes/$lang/teams/ apps/web/src/routes/$lang/users/
git commit -m "feat: i18n refactor for profile, login, landing, tournaments, teams, users routes"
```

---

## Task 11: Refactor admin route files to use i18n

**Files:**
- Modify: `apps/web/src/routes/$lang/admin/tournaments/index.tsx`
- Modify: `apps/web/src/routes/$lang/admin/tournaments/$tournamentId/matches.tsx`
- Modify: `apps/web/src/routes/$lang/admin/teams.tsx`
- Modify: `apps/web/src/routes/$lang/admin/users.tsx`
- Modify: `apps/web/src/routes/$lang/admin/compensations.tsx`
- Modify: `apps/web/src/routes/$lang/admin/live/$matchId.tsx`
- Modify: `apps/web/src/routes/$lang/admin/migrate-logos.tsx`

- [ ] **Step 1: Refactor admin tournaments index**

Use `useTranslation("admin")`:

```
"Limites de underdog invalidos..." → t("tournaments.underdogLimits", { details })
"Bonus do tier 1 de underdog" → t("tournaments.tier1Bonus")
"Bonus do tier 2 de underdog" → t("tournaments.tier2Bonus")
"Tier 1 ate (%)" → t("tournaments.tier1Threshold")
"Tier 2 ate (%)" → t("tournaments.tier2Threshold")
"Limite maximo de votos" → t("tournaments.maxVotes")
"Fallback automatico" → t("tournaments.autoFallback")
"Esta logo esta em Base64..." → t("tournaments.base64Warning")
"Pagina de Migracao" → t("common.logoMigrationPage")
```

- [ ] **Step 2: Refactor admin matches**

Use `useTranslation("admin-matches")`:

```
"Crie e organize as partidas..." → t("matchBuilder.description")
"Erro ao recalcular pontuacao..." → t("recalc.error")
"Recalculo concluido..." → t("recalc.success", { matchCount, betCount })
"Tem certeza que deseja resetar..." → t("reset.confirm")
"ATENCAO: Ja existem partidas..." → t("matchBuilder.existingMatchesWarning")
"Deseja continuar?" → t("matchBuilder.continueQuestion")
"Gerencie as partidas..." → t("matchBuilder.manageGroupMatches")
```

- [ ] **Step 3: Refactor admin teams**

Use `useTranslation("admin")`:

```
"Time excluido!" → t("teams.toast.deleted")
"Erro ao salvar time" → t("teams.toast.saveError")
"Erro ao excluir time" → t("teams.toast.deleteError")
"O arquivo e muito grande!..." → t("teams.logoTooLarge")
"Regiao" → t("teams.region")
"Confirmar Exclusao" → t("teams.deleteConfirm")
"Nao, Cancelar" → t("teams.deleteCancel")
```

- [ ] **Step 4: Refactor admin compensations**

Use `useTranslation("admin")`:

```
"COMPENSACAO DE PONTOS" → t("compensations.title")
"Nova Compensacao" → t("compensations.new")
"Usuario" → t("compensations.user")
"Buscar usuario por nome..." → t("compensations.searchUser")
"Descreva o motivo..." → t("compensations.reasonPlaceholder")
"Confirmar Compensacao" → t("compensations.confirm")
"Esta acao sera registrada..." → t("compensations.auditNote")
"Esta acao nao pode ser desfeita..." → t("compensations.irreversible")
"Historico de Ajustes" → t("compensations.history")
"Ajuste manual de pontos..." → t("compensations.description")
```

- [ ] **Step 5: Refactor admin live match**

Use `useTranslation("admin-matches")`:

```
"O placar esta empatado..." → t("live.tieWarning")
"Erro: Times nao definidos" → t("live.teamsError")
"Voce comecou a pontuar..." → t("live.scoringStarted")
"Esta acao nao pode ser desfeita..." → t("live.irreversibleWarning")
"O que deseja fazer..." → t("live.whatToDo")
```

- [ ] **Step 6: Refactor admin migrate-logos**

Use `useTranslation("admin")`:

```
"O que sera feito?" → t("migrateLogos.description")
"Atencao: Este processo..." → t("migrateLogos.warning")
"Iniciar Migracao" → t("migrateLogos.start")
"Executando..." → t("migrateLogos.running")
"Migracao Concluida" → t("migrateLogos.success")
"Erro na Migracao" → t("migrateLogos.error")
"Concluido em: ..." → t("migrateLogos.completedAt", { time })
```

- [ ] **Step 7: Refactor admin users**

Check for any Portuguese strings (may have none or minimal). If any found, use `useTranslation("admin")` with appropriate keys from `users` or `common`.

- [ ] **Step 8: Commit**

```bash
git add apps/web/src/routes/$lang/admin/
git commit -m "feat: i18n refactor for all admin routes"
```

---

## Task 12: Refactor component files to use i18n (part 1 — GlobalHeader, user-menu, LandingPage, MatchCard, bracket/MatchCard)

**Files:**
- Modify: `apps/web/src/components/GlobalHeader.tsx`
- Modify: `apps/web/src/components/user-menu.tsx`
- Modify: `apps/web/src/components/LandingPage.tsx`
- Modify: `apps/web/src/components/MatchCard.tsx`
- Modify: `apps/web/src/components/bracket/MatchCard.tsx`

- [ ] **Step 1: Refactor GlobalHeader.tsx**

Add `useTranslation("common")`:

```
"Usuarios" → t("nav.adminUsers")
"Compensacao" → t("nav.adminCompensations")
```

Also update navigation links to include lang prefix. Links that were `/dashboard` become `/${lang}/dashboard`. Use the `lang` from `useParams()` or derive from `i18next.language`.

- [ ] **Step 2: Refactor user-menu.tsx**

Use `useTranslation("profile")`:

```
"Perfil do Usuario" → t("userMenu.label")
"Ver Perfil Publico" → t("userMenu.viewPublic")
```

Update links to include `/${lang}/` prefix.

- [ ] **Step 3: Refactor LandingPage.tsx**

Use `useTranslation("landing")`:

```
"Faca seus palpites e suba no ranking" → t("hero.title")
"voce entende do competitivo." → t("features.predictDesc")
"Acumule pontos com seus palpites certeiros..." → t("features.scoreDesc")
"PREMIO" → t("features.prizes")
```

Links to `/login` and `/dashboard` must include lang prefix.

- [ ] **Step 4: Refactor MatchCard.tsx**

Use `useTranslation("betting")`:

```
"Voce apostou em:" → t("betLabel")
"Bonus azarao (+25%)" → t("bonus.underdog", { percent: 25 })
"Total: {{N}} pontos" → t("totalPoints", { count: N })
```

- [ ] **Step 5: Refactor bracket/MatchCard.tsx**

Same as MatchCard above, plus:

```
"Todos os apostadores recebem pontos de vencedor" → t("walkoverNote")
"Partida encerrada por walkover" → t("walkoverTitle")
```

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/components/GlobalHeader.tsx apps/web/src/components/user-menu.tsx apps/web/src/components/LandingPage.tsx apps/web/src/components/MatchCard.tsx apps/web/src/components/bracket/MatchCard.tsx
git commit -m "feat: i18n refactor for GlobalHeader, user-menu, LandingPage, MatchCards"
```

---

## Task 13: Refactor component files to use i18n (part 2 — remaining components)

**Files:**
- Modify: `apps/web/src/components/BettingCarousel.tsx`
- Modify: `apps/web/src/components/MatchDaySelector.tsx`
- Modify: `apps/web/src/components/TournamentPodium.tsx`
- Modify: `apps/web/src/components/TournamentSelector.tsx`
- Modify: `apps/web/src/components/MedalSummary.tsx`
- Modify: `apps/web/src/components/GSLResultView.tsx`
- Modify: `apps/web/src/components/TeamLogo.tsx`
- Modify: `apps/web/src/components/admin/MatchModal.tsx`
- Modify: `apps/web/src/components/admin/MatchDaysManager.tsx`
- Modify: `apps/web/src/components/admin/MatchOrdering.tsx`

- [ ] **Step 1: Refactor BettingCarousel.tsx**

Use `useTranslation("betting")`:

```
"Proximo Jogo" → t("nextMatch")
"Os jogos de hoje ja comecaram..." → t("empty.noMatches")
"Regiao" → t("common:region")
"Serie" → t("common:series")
```

- [ ] **Step 2: Refactor MatchDaySelector.tsx**

Use `useTranslation("tournament")`:

```
"Concluido" → t("matchDay.completed")
"Escolha qual dia de partidas..." → t("matchDay.selectPrompt")
"disponivel" → t("matchDay.available")
"disponiveis" → t("matchDay.availablePlural")
"Nenhuma partida atribuida" → t("matchDay.noMatches")
"Aguardando atualizacao" → t("matchDay.pending")
"Nao ha match days disponiveis..." → t("matchDay.noMatchDay")
```

- [ ] **Step 3: Refactor TournamentPodium.tsx**

Use `useTranslation("tournament")`:

```
"Podio Final" → t("podium.title")
"CAMPEAO" → t("podium.champion")
```

- [ ] **Step 4: Refactor TournamentSelector.tsx**

Use `useTranslation("tournament")`:

```
"RECUPERACAO" → t("selector.recovery")
```

Also add any other Portuguese strings found.

- [ ] **Step 5: Refactor MedalSummary.tsx**

Use `useTranslation("user")`:

```
"Ultimas Conquistas" → t("medals.latest")
```

- [ ] **Step 6: Refactor GSLResultView.tsx**

Use `useTranslation("tournament")`:

```
"eliminacao" → t("detail.elimination")
```

- [ ] **Step 7: Refactor TeamLogo.tsx**

Portuguese comments only — replace or remove. No user-facing strings to translate.

- [ ] **Step 8: Refactor admin/MatchModal.tsx**

Use `useTranslation("admin-matches")`:

```
"Falha ao salvar atualizacao da partida" → t("modal.saveError")
"W.O. com um unico time..." → t("modal.woDescription")
"Vencedor do W.O. atualizado..." → t("modal.woUpdated")
"Limpar vencedor" → t("modal.clearWinner")
```

- [ ] **Step 9: Refactor admin/MatchDaysManager.tsx**

Use `useTranslation("admin-matches")`:

```
"Dia excluido." → t("matchDays.deleted")
"Dia criado com sucesso!" → t("matchDays.created")
"Erro ao criar dia." → t("matchDays.createError")
"Erro ao excluir dia." → t("matchDays.deleteError")
"Erro ao atualizar match day." → t("matchDays.updateError")
"Draft = Ainda nao disponivel" → t("matchDays.draft")
```

- [ ] **Step 10: Refactor admin/MatchOrdering.tsx**

Use `useTranslation("admin-matches")`:

```
"Ordem salva com sucesso!" → t("ordering.saved")
"Erro ao salvar ordem" → t("ordering.saveError")
```

- [ ] **Step 11: Commit**

```bash
git add apps/web/src/components/
git commit -m "feat: i18n refactor for all remaining components"
```

---

## Task 14: Refactor server functions to use i18n

**Files:**
- Modify: `apps/web/src/server/bets.ts`
- Modify: `apps/web/src/server/tournaments.ts`
- Modify: `apps/web/src/server/teams.ts`
- Modify: `apps/web/src/server/point-adjustments.ts`
- Modify: `apps/web/src/server/users.ts`
- Modify: `apps/web/src/utils/validators.ts`

- [ ] **Step 1: Create server i18n helper with sync API**

Since server functions need synchronous translation access, update `apps/web/src/i18n/index.ts` to add a sync helper. The pre-built `i18next` instance with loaded resources can use `getFixedT` synchronously:

Add to `apps/web/src/i18n/index.ts`:

```typescript
// Synchronous t-function for server use.
// Resources are pre-imported in config.ts, so getFixedT works synchronously after init.
export function createServerT(lang: SupportedLang) {
	const instance = i18next.cloneInstance();
	instance.init({
		...i18nOptions,
		lng: lang,
	});
	return instance.getFixedT(lang, null);
}
```

- [ ] **Step 2: Refactor server/bets.ts**

For each server function, add `lang` to the input schema with `.default("pt")` and use `createServerT`:

```typescript
import { createServerT } from "@/i18n";
import type { SupportedLang } from "@/i18n/config";
```

Before the handler, add:
```typescript
.lang: z.enum(["pt", "en"]).default("pt"),
```

In the handler:
```typescript
const t = createServerT(input.lang as SupportedLang);
```

Replace all Portuguese error/validation strings:
```
"Usuario nao autenticado" → t("errors:unauthenticated")
"Partida nao encontrada" → t("errors:matchNotFound")
"Apostas encerradas..." → t("errors:bettingClosed")
"Apostas desabilitadas..." → t("errors:bettingDisabled")
"Vencedor previsto deve ser..." → t("errors:invalidWinner")
"Vencedor invalido para partida" → t("errors:invalidWinnerAlt")
"O vencedor deve ter mais pontos..." → t("errors:winnerMustHaveMorePoints")
"Erros de validacao" → t("errors:validationErrors")
"aposta(s) salva(s) com sucesso" → t("errors:betsSaved")
"Partida indisponivel para recovery..." → t("errors:recoveryUnavailable")
"Aposta de recuperacao ja foi realizada..." → t("errors:recoveryAlreadyPlaced")
"Match ID deve ser um numero positivo" → t("validation:matchIdPositive")
"Winner ID deve ser um numero positivo" → t("validation:winnerIdPositive")
```

- [ ] **Step 3: Refactor server/tournaments.ts**

Add `lang` to input schemas, use `createServerT`:

```
"Nome e obrigatorio" → t("validation:nameRequired")
"Slug e obrigatorio" → t("validation:slugRequired")
"Deve ser uma URL valida..." → t("validation:imageUrlRequired")
```

- [ ] **Step 4: Refactor server/teams.ts**

Add `lang` to input schemas, use `createServerT`:

```
"Nome e obrigatorio" → t("validation:nameRequired")
"Time nao encontrado" → t("errors:teamNotFound")
"Nao e possivel excluir este time..." → t("errors:cannotDeleteTeam", { tournaments })
```

- [ ] **Step 5: Refactor server/point-adjustments.ts**

Add `lang` to input schemas, use `createServerT`:

```
"Nao autenticado" → t("errors:unauthenticated")
"Usuario nao encontrado" → t("errors:userNotFound")
"Torneio nao encontrado" → t("errors:tournamentNotFound")
"Partida nao encontrada" → t("errors:matchNotFound")
"Erro ao buscar ajustes de pontos" → t("errors:pointAdjustmentError")
"Apenas administradores podem ajustar pontos" → t("errors:adminOnly")
"Apenas administradores podem buscar usuarios" → t("errors:adminOnlySearch")
"Usuario" (fallback label) → t("errors:pointAdjustmentFallbackUser")
```

- [ ] **Step 6: Refactor server/users.ts**

Add `lang` to input schemas, use `createServerT`:

```
"Nao autenticado" → t("errors:unauthenticated")
"Conta Google nao encontrada" → t("errors:googleAccountNotFound")
"Nao foi possivel obter o avatar..." → t("errors:googleAvatarError")
```

- [ ] **Step 7: Refactor utils/validators.ts**

Import `createServerT` from `@/i18n`. For server-called validators, accept a `lang` parameter. For client-side callers, they'll need to pass the lang or use the i18next instance.

Add `lang` parameter to validation functions that return Portuguese error messages:

```typescript
import { createServerT } from "@/i18n";
import type { SupportedLang } from "@/i18n/config";

export function validateScore(/* existing params */, lang: SupportedLang = "pt") {
	const t = createServerT(lang);
	// Use t() for error messages
}

// Replace:
"O placar nao pode ser negativo" → t("validation:negativeScore")
"Maximo de {N} sets neste formato" → t("validation:maxSets", { n })
"Alguem precisa vencer {N} sets" → t("validation:someoneMustWin", { n })
```

- [ ] **Step 8: Commit**

```bash
git add apps/web/src/server/ apps/web/src/utils/validators.ts apps/web/src/i18n/index.ts
git commit -m "feat: i18n refactor for server functions and validators"
```

---

## Task 15: Update test files

**Files:**
- Modify: `apps/web/src/utils/recovery-bug.test.ts`
- Modify: `apps/web/src/utils/recovery-integration.test.ts`

- [ ] **Step 1: Translate test descriptions to English**

The test files `recovery-bug.test.ts` and `recovery-integration.test.ts` have test descriptions entirely in Portuguese. Translate them to English for consistency with the codebase:

```
"BUG: Recovery betting quando usuario erra todas as apostas das quartas"
→ "BUG: Recovery betting when user misses all quarterfinal bets"

"deve permitir recovery na semifinal quando usuario erra ambas as quartas"
→ "should allow recovery in semifinal when user misses both quarterfinals"

"Cenario adicional: Usuario nao fez NENHUMA aposta nas quartas"
→ "Additional scenario: User made NO bets in quarterfinals"

"BUG: Recovery em torneio com multiplos match days"
→ "BUG: Recovery in tournament with multiple match days"

"usuario deve ver SF como editable quando seleciona MD das semis"
→ "user should see SF as editable when selecting semifinal MD"

"usuario NAO ve SF quando esta no MD das quartas - ISSO PODE SER O BUG!"
→ "user does NOT see SF when on quarterfinal MD - THIS COULD BE THE BUG!"
```

- [ ] **Step 2: Commit**

```bash
git add apps/web/src/utils/recovery-bug.test.ts apps/web/src/utils/recovery-integration.test.ts
git commit -m "refactor: translate test descriptions to English"
```

---

## Task 16: Update navigation links across all components

**Files:**
- All route files and components that contain `to="/..."` navigation links

Every `<Link to="/...">` must include the `$lang` segment. Components need to derive the current language and prefix links.

- [ ] **Step 1: Create a link helper hook**

Create `apps/web/src/i18n/useLangLink.ts`:

```typescript
import { useTranslation } from "react-i18next";

export function useLangLink() {
	const { i18n } = useTranslation();
	const lang = i18n.language;

	function linkTo(path: string) {
		const cleanPath = path.startsWith("/") ? path : `/${path}`;
		return `/${lang}${cleanPath}`;
	}

	return { lang, linkTo };
}
```

- [ ] **Step 2: Update all Link components**

In all files with `<Link to={...}>`, import `useLangLink` and wrap the `to` value:

```typescript
const { linkTo } = useLangLink();
// <Link to="/dashboard"> becomes <Link to={linkTo("/dashboard")}>
```

Files to update:
- `apps/web/src/components/GlobalHeader.tsx`
- `apps/web/src/components/user-menu.tsx`
- `apps/web/src/components/LandingPage.tsx`
- All `$lang/*.tsx` route files that have `<Link>` components

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/i18n/useLangLink.ts apps/web/src/components/GlobalHeader.tsx apps/web/src/components/user-menu.tsx apps/web/src/components/LandingPage.tsx
git commit -m "feat: add useLangLink helper, update navigation links with lang prefix"
```

---

## Task 17: Build, typecheck, lint, and test

**Files:**
- None (verification only)

- [ ] **Step 1: Regenerate route tree**

The TanStack Router CLI must regenerate `routeTree.gen.ts` after the route restructuring.

Run: `bun run turbo -F web dev` and check logs for route tree generation, then kill the dev server. Or use the router plugin CLI directly (TBD based on project setup).

If the project has a dedicated command for route generation, run it. Otherwise, running `bun run dev:web` for a few seconds should trigger auto-generation of `apps/web/src/routeTree.gen.ts`.

- [ ] **Step 2: Run typecheck**

```bash
bun run check-types
```

Expected: Zero type errors. Fix any type issues (likely: missing imports in moved route files, wrong paths after file moves).

- [ ] **Step 3: Run Biome lint + format**

```bash
bun run check
```

Expected: Zero lint errors, all formatting applied. Fix any issues.

- [ ] **Step 4: Run tests**

```bash
bun test
```

Expected: All existing tests pass.

- [ ] **Step 5: Run build**

```bash
bun run build
```

Expected: Build succeeds with no errors.

- [ ] **Step 6: Commit final fixes**

```bash
git add -A
git commit -m "fix: typecheck, lint, and build fixes after i18n migration"
```

---

## Post-Implementation Verification

After all tasks complete, manually verify:

1. **`/` redirects to `/pt/`** (or `/en/` if browser locale is English — depends on detection)
2. **`/pt/` shows Portuguese UI** — all strings appear in Portuguese
3. **`/en/` shows English UI** — all strings appear in English
4. **Invalid lang `/fr/` redirects to `/pt/`**
5. **Server errors** appear in the correct language (test: try to bet on a finished match while on `/en/`)
6. **Pluralization works**: "1 bet" vs "2 bets" vs "1 palpite" vs "2 palpites"
7. **Date formatting** uses the correct locale
8. **No layout breakage** with longer English text (especially on mobile)
9. **All navigation links** include the correct lang prefix
10. **HTML lang attribute** changes between `/pt/` and `/en/`
